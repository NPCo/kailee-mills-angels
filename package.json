{
  "name": "kailee-mills-angels",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "body-parser": "^1.18.3",
    "cors": "^2.8.5",
    "dotenv": "^6.1.0",
    "express": "^4.16.4",
    "helmet": "^3.15.0",
    "joi": "^14.3.0",
    "monk": "^6.0.6",
    "morgan": "^1.9.1"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "cd client && npm run build && cd .. && node index.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "devDependencies": {},
  "engines": {
    "node": "10.1.0"
  }
}
